Practice Qs

Q1. For the Give String:
let msg = "help!";
Trim it & convert it to uppercase.

Q2. For the string -> let name = "AppnaCollege", predict the output for following :
name.slice(4,9)
name.indexof("na")
name.replace("Apna","Our")

Q3. Separate the "College" part in above string & replace 'l' with 't' in it.